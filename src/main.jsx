import React from 'react' //API de REACT
import ReactDOM from 'react-dom/client' // Tiene todo los eventos del DOM
import './index.css'
import App from './App'

ReactDOM.createRoot(document.getElementById('root')).render(
  
    <App />
  
)
